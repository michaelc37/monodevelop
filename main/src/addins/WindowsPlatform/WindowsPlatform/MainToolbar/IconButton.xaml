<UserControl x:Class="WindowsPlatform.MainToolbar.IconButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsPlatform.MainToolbar"
             xmlns:i="clr-namespace:WindowsPlatform"
             mc:Ignorable="d">
    <Button Click="OnClick" FocusVisualStyle="{x:Null}" UseLayoutRounding="True">
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <StackPanel UseLayoutRounding="True">
                    <ContentPresenter />
                </StackPanel>
            </ControlTemplate>
        </Button.Template>
    	<i:ImageBox x:Name="RunIcon" Image="{Binding Image}" UseLayoutRounding="True" />
    </Button>
</UserControl>
